<%= form_with(model: lift_req, local: true) do |form| %>
  <% if lift_req.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lift_req.errors.count, "error") %> prohibited this lift_req from being saved:</h2>

      <ul>
      <% lift_req.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :example_id %>
    <%= form.text_field :example_id, id: :lift_req_example_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
