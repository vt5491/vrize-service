<p id="notice"><%= notice %></p>
<p>

<table>
  <thead>
    <tr>
      <th>id</th>
      <th class="left-align">tag.id</th>
      <!-- <th class="left-align">example.id</th> -->
      <th class="left-align">tag.tag</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @tags.each do |tag| %>
      <tr>
        <td><%= tag.id %></td>
        <td><%= tag.tag %></td>
      </tr>
    <% end %>
  </tbody>
</table>
